ruby:
  data = {
    totals: report.totals.to_a,
    colors: report.colors
  }

div id="donut-chart" data-json="#{data.to_json}"

== javascript_include_tag webpack_url('javascripts/views/shared/donut-chart.js'), media: :all
