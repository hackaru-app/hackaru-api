en:
  activerecord:
    models:
      must_have_survey: 'User survey'
    errors:
      models:
        must_have_survey:
          attributes:
            user_id:
              taken: 'has already answered'
