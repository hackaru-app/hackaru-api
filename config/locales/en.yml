# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# The following keys must be escaped otherwise they will not be retrieved by
# the default I18n backend:
#
# true, false, on, off, yes, no
#
# Instead, surround them with single quotes.
#
# en:
#   'true': 'foo'
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  activerecord:
    errors:
      messages:
        is_invalid: 'is invalid.'
        cannot_be_before_started_at: 'can not be before started at.'
  rack_attack:
    throttled:
        message: 'Retry later.'
        status: 429
  user_mailer:
    password_reset:
      subject: Reset Password instruction
      greeting: Hello, %{recipient}！
      instruction: Someone has requested a reset your password.
      instruction_2: You can click the link below if you want reset password.
      instruction_3: If you didn't request this, please remove this email.
  error_renderer:
    sign_in_failed:
      message: 'Invalid email or password.'
      status: 401
    access_token_invalid:
      message: 'Authenticate failed, please sign in before continuing.'
      status: 401
    refresh_token_invalid:
      message: 'Refresh token is invalid or expired, please sign in before continuing.'
      status: 401
    password_reset_token_invalid:
      message: 'The request is invalid or expired, please request a new one.'
      status: 401
    current_password_invalid:
      message: 'Current password is invalid.'
      status: 422
    email_not_found:
      message: 'The email is not registration.'
      status: 404
  exception_renderer:
    default:
      message: 'We''re sorry, but something went wrong.'
      status: 500
    argument_error:
      message: 'The request has invalid parameter.'
      status: 422
    active_record:
      record_not_found:
        message: 'The requested resouce is not exists.'
        status: 404
      record_invalid:
        status: 422
      not_null_violation:
        message: 'The request is missing a required parameter.'
        status: 400
    action_controller:
      parameter_missing:
        message: 'The request is missing a required parameter.'
        status: 400
      routing_error:
        message: 'The requested resouce is not exists.'
        status: 404
    groupdate:
      error:
        message: 'The request has invalid parameter.'
        status: 422
    rails_param:
      param:
        invalid_parameter_error:
          message: 'The request has invalid parameter.'
          status: 422
